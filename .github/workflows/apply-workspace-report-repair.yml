name: Apply workspace and report repair

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-repair:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "One-shot repair already applied."
